{
  "version": 1,
  "artifact_store": {
    "enabled": true,
    "root_directory": "/home/cmd_mcp/artifacts",
    "max_upload_bytes": 524288000,
    "max_chunk_bytes": 524288
  },
  "deploy_profiles": [
    {
      "id": "ari-bot",
      "description": "Deploy an uploaded ari-bot linux tar.gz package to /var/www/html/ari-bot.",
      "artifact_name_pattern": "^ari-bot-[a-zA-Z0-9][a-zA-Z0-9._-]*\\.(tar\\.gz|tgz)$",
      "deploy_command_id": "deploy_ari_bot"
    }
  ],
  "commands": [
    {
      "id": "deploy_ari_bot",
      "description": "Run the fixed ari-bot deployment helper for a committed tar.gz artifact.",
      "enabled": true,
      "risk_level": "high",
      "access": "state_changing",
      "executable": "/opt/server-shell-mcp/scripts/deploy-ari-bot.sh",
      "argv_template": [
        {
          "param": "artifact_path"
        }
      ],
      "working_directory": "/home/cmd_mcp",
      "environment": {
        "mode": "minimal",
        "variables": {
          "LANG": "C"
        }
      },
      "parameters": {
        "artifact_path": {
          "type": "path",
          "required": true,
          "description": "Committed ari-bot artifact path generated by the MCP artifact store.",
          "allowed_roots": [
            "/home/cmd_mcp/artifacts/committed"
          ],
          "mode": "read_only",
          "must_exist": true
        }
      },
      "execution": {
        "timeout_ms": 180000,
        "max_output_bytes": 16384,
        "per_command_limit": 1,
        "global_limit": 1
      },
      "output": {
        "stdout": "text",
        "stderr": "text",
        "truncate": true
      },
      "audit": {
        "event_name": "command.deploy_ari_bot",
        "redact_parameters": [],
        "include_stdout": false,
        "include_stderr": false,
        "include_rejection": true
      }
    },
    {
      "id": "read_ari_bot_logs",
      "description": "List ari-bot log files under /var/www/html/ari-bot/logs or read the tail of one log file for troubleshooting. Arguments: action (required, 'list' shows files with mtime and size, newest first; 'tail' reads one file); file (path under /var/www/html/ari-bot/logs, used by tail only, defaults to the logs root which list ignores); lines (integer 1-2000, tail only, default 200); keywords (array of 1-5 plain strings, tail only, case-insensitive OR filter, e.g. [\"error\", \"timeout\"]).",
      "enabled": true,
      "risk_level": "low",
      "access": "read_only",
      "executable": "/opt/server-shell-mcp/scripts/read-ari-bot-log.sh",
      "argv_template": [
        {"param": "action"},
        {"param": "file"},
        {"param": "lines"},
        {"repeat": {"param": "keywords", "prefix": "-e"}}
      ],
      "working_directory": "/home/cmd_mcp",
      "environment": {
        "mode": "minimal",
        "variables": {
          "LANG": "C"
        }
      },
      "parameters": {
        "action": {
          "type": "enum",
          "required": true,
          "values": ["list", "tail"],
          "description": "list = show available log files; tail = read the last lines of one log file."
        },
        "file": {
          "type": "path",
          "required": false,
          "default": "/var/www/html/ari-bot/logs",
          "description": "Log file to read (tail only). Must stay under /var/www/html/ari-bot/logs.",
          "allowed_roots": [
            "/var/www/html/ari-bot/logs"
          ],
          "mode": "read_only"
        },
        "lines": {
          "type": "int",
          "required": false,
          "default": 200,
          "min": 1,
          "max": 2000,
          "description": "Number of trailing lines to read (tail only)."
        },
        "keywords": {
          "type": "array",
          "required": false,
          "description": "Case-insensitive fixed-string keywords to keep, OR-combined (tail only).",
          "item": {
            "type": "string",
            "min_length": 1,
            "max_length": 128,
            "pattern": "^[A-Za-z0-9 _.,:@/()\\[\\]'\"*+?^~!=%-]+$"
          },
          "max_items": 5
        }
      },
      "execution": {
        "timeout_ms": 10000,
        "max_output_bytes": 32768,
        "per_command_limit": 2,
        "global_limit": 4
      },
      "output": {
        "stdout": "text",
        "stderr": "text",
        "truncate": true
      },
      "audit": {
        "event_name": "command.read_ari_bot_logs",
        "redact_parameters": [],
        "include_stdout": false,
        "include_stderr": false,
        "include_rejection": true
      }
    }
  ]
}

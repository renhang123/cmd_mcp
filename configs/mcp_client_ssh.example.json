{
  "mcpServers": {
    "server-shell-prod": {
      "command": "ssh",
      "args": [
        "-T",
        "-i", "/Users/niklaus/.ssh/server_shell_mcp_prod",
        "-o", "IdentitiesOnly=yes",
        "-o", "BatchMode=yes",
        "-o", "LogLevel=ERROR",
        "-o", "ServerAliveInterval=30",
        "cmd_mcp@192.168.3.13"
      ]
    }
  }
}
